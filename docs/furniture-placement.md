# 家具の配置

部屋タブ・つくるタブの部屋ステージでは、表示中の家具を直接ドラッグして配置できます。

- 左右・上下の移動に対応。
- 部屋の外へ完全にはみ出さないように端を制限。
- 指を離した位置を `AsyncStorage` に保存。
- 次回起動時も家具ごとの位置を復元。

位置は家具IDごとに `{ left, bottom }` のパーセントで保存しています。古い保存データには位置情報がないため、各家具の初期配置へ自動的にフォールバックします。

