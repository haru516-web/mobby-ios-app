/**
 * Keep disabled features behind a single reversible switch so their saved data
 * can remain intact while no UI or background trigger affects the app.
 */
export const INCIDENTS_ENABLED = false;
